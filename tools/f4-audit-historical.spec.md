# F4 historical transcript audit

Apply to every operator transcript. Debug dumps are not operator
transcripts.

## sheet

```text
session-id:
tag:
processes: fresh / reused / restored-checkpoint
cold-start taught / episodes / policies / packs:
regime: A library-only | B research one-shot | C cumulative
contamination: yes/no
  if yes: taught-at-boot / shared-checkpoint / curriculum-in-B / oracle-in-prompt
audit-header loaded-class list: present/missing
teaches:
  each: residual-cited yes/no; circular yes/no; computable yes/no
F3 vs decoy: identical | different | decoy-not-run
instrument class: pre-D11 backward-blind
```

Missing loaded-class list in the header is a defect.

## not operator transcripts

```text
cold_debug_stage1_runtime.log
cold_debug_stage1_runtime_long.log
engel_e1_prove.debug.log
```

## applied sheets

See `logs/f4-historical-audits.txt`.
