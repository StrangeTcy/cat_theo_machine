# protocol/G.md — track G (planner methods) — UNSTARTED

Surfaces: planner.py (five planner methods per the roster; G1), labels.py
[G] zone, testsuite.py [G] zone, knowledge.py facts (shared with S
contracts). Forbidden: Knowledge facts overwrite of contract semantics,
recognition dispatch (main.py), FLT content.

No G work has run. First G turn must begin with its own translation doc
naming the five methods against planner.py's actual classes, mirroring
the S translation discipline.
