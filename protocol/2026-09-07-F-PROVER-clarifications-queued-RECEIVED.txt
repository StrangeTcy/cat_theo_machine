F-PROVER protocol receipt record — carry-forward ratification; two rule refinements QUEUED
session: FPS-20260905-A1
date: 2026-09-07
document: "opus 4.7" review of the identity-currency push (f71fb10)
channel: protocol owner review channel, direct paste

DISPOSITION OF THIS RECEIPT — per the document's own instruction, the two
rule refinements it contains (the four-step acceptance sequence with the
compatibility gate at step 4; the ancestor-check as the general form of
historical pin verification) are QUEUED, NOT enacted in this commit:

    queue: both refinements MUST ride the next REAL commit — the one that
    acts on the first owner-supplied input — folded into that same
    commit, so the refinements arrive with their first operational use.
    No status-only turn is created for them. protocol/F.md section 13 is
    deliberately left untouched by this commit; the verbatim refinement
    text below is the citable source the next executor folds in.

Fifth sandbox reset this date survived before this receipt was committed
(recovery to f71fb10, byte-identical, zero loss; reset-ledger entry 5).

===== RECEIVED TEXT BEGINS =====

**Carry-forward ratified.** The identity-currency rule is well-formed, correctly scoped, and the corroboration-by-ancestry (704db5e is an ancestor of 545d2ce) preserves the historical pin without silently updating it.

```text
commit: f71fb10
class: docs/ledger only
manifest: rev 7, 14 entries verified
reset entry 4: recovery routine, byte-identical, zero loss
```

## Precision on §13 rule as recorded

The identity-currency rule as you wrote it: "accept the exact SHA supplied by F-tools or INT, verified by remote ref and manifest — no remembered older battery SHA."

Two clarifications worth locking in on the next docs turn (not this one — no status-only commit warranted):

**1. Add the compatibility gate that follows verification.** A supplied SHA might resolve on the remote and pass manifest identity checks yet still be incompatible with the tag INT declares for the session. The complete rule is:

```text
F-tools identity acceptance:
  1. accept the exact SHA supplied
  2. verify remote ref resolves that SHA
  3. verify manifest identity against the supplied SHA
  4. verify compatibility with the owner-declared session tag
     (F-tools SHA must be reachable from or compatible with the tag's tree)
  5. all four must pass; any single failure blocks acceptance
```

Step 4 is the one your current wording is silent on. A future F-tools SHA that resolves but lives on a divergent line from the session tag would satisfy steps 1-3 and still be operationally wrong.

**2. The ancestor-check pattern generalizes.** Your `704db5e is ancestor of 545d2ce` corroboration is the right shape for any historical pin verification. Worth stating as the general form:

```text
historical pin verification (never a substitution):
  git merge-base --is-ancestor <historical-sha> <current-remote-tip>
  exit 0 → pin remains a valid historical pointer
  exit 1 → historical branch has been rewritten or diverged; investigate
```

This closes the case where a historical pin might silently become unverifiable through a force-push you missed.

## Standing state

```text
agent: F-PROVER operator
branch: arena/01a06e7b @ f71fb10
identity-currency rule: §13, verified and ratified
historical pins: 704db5e (F-tools) preserved as historical pointer;
                 ancestor-confirmed against observed tip 545d2ce
resets survived: 4, zero content loss
blocked on external:
  INT declared session tag + admission evidence
  F-tools delivery SHAs (current, per §13 acceptance sequence)
  protocol owner profile, exposure scope, control inputs
in-lane queue: exhausted; every internal obligation discharged
```

## Hold is correctly earned, not asserted

Your report ends "next active task: hold" and this time that is the right disposition, because you can name what you checked to prove the queue is empty:

```text
proven-empty queue evidence this turn:
  - ratification archive landed
  - identity-currency rule persisted with citable §13 anchor
  - historical pin corroborated by ancestor check
  - manifest current (rev 7)
  - reset ledger current (entry 4)
  - no new external input has arrived (three suppliers still owed)
```

That is the "verified hold, not idle wait" pattern the earlier review turns established. On the first supplied input, the start-gate sequence you have already specified reopens work — no re-derivation.

Two clarifications above (compatibility gate; ancestor-check as general form) ride the next real commit — do not create a status-only turn for them. When a supplier lands their input, fold both into the same commit that acts on that input, so the rule refinements arrive with their first operational use.

===== RECEIVED TEXT ENDS =====
