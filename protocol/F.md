# F-op gate (not an engineering gate)

Decoy shape-match lives here. It gates **F-op sessions only**.

S, E, G, and F-tools engineering do not wait on this file.

## Current terms

FLT goal term: ABSENT on this tip.

decoy goal term: ABSENT on this tip.

predicate arity: incomparable (both missing)

case-split shape: incomparable (both missing)

exponent position: incomparable (both missing)

`DECOY REDESIGN REQUIRED` for F-op only.

Do not start an F-op session until both terms exist and match on arity,
case-split shape, and exponent position. INT does not redesign the decoy.
