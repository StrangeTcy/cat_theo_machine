# D12 — AuditHeaderOmitsLoadedClasses

```text
D12 — AuditHeaderOmitsLoadedClasses
locus:      research-mode `audit knowledge` output, first audit of a session
observed:   eight first audits carry no loaded-class list
            (three blank controls, three blinds, toy-live-protocol,
             ground-evaluation first audit)
class:      instrument-level, non-semantic (output completeness, not search)
consequence:
            F4 rule "audit header lists every loaded class" cannot be
            verified on those eight sessions from the transcript alone;
            they are admissible only as pre-D11 backward-blind instrument
            records, which is the class the contamination ledger already
            assigns them
routing:    INT [SHARED]; fix lands with the research.py-bearing cut
            alongside F1 verbs, since both touch the same audit surface
logs:       not rewritten (correct)
```

Source sheets: `logs/f4-historical-audits.txt`
Contract: `tools/f1-checkpoint-verbs.spec.md` (header must print loaded classes)
