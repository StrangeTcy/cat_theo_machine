# Track S -- self-improvement -- contracted relation schemas

Ledger entries for this track. Newest last. See protocol/README.md for
the rules governing entries, and research_protocol.md for cross-track
discipline.

---

## referred from F: ZeroPartialMatchAmbiguity

```text
source: protocol/F.md session-pair F-OP-BLIND-FLT-RETRY
tag: experiment-5-frozen
kind: instrument concern, not a theorem gap
name: ZeroPartialMatchAmbiguity
symptom: genuine partial match 0 / 0 on a shape-matched decoy and on FLT
         cost 334 / 334; attempted operational rules 0; UncharacterizedStall
question: empty partial-match because no rule could match this shape,
          or because the partial-match criterion is too coarse to
          register a candidate
nosolutions on this tag: zero pack rules; F loaded packs only, taught 0;
                         attempted-rules 0 is absence, not a rejection
status: named, not solved
owner: S/INT
```

F withdraws the FLT reading until a decoy/FLT residual pair is
distinguishable. Matcher refinement that makes the two walls differ —
even if both still fail — is the instrument condition F3 needs. Until
then further F sessions on this pair produce the same null.

Evidence update (F, same session-pair): the decoy zero is not
attributable to pre-flight item 2. Decoy inner heads include `eq` and
`pow`, which exist in the 167-rule library; decoy contains no
`nosolutions`. Missing producer/consumer can explain at most the FLT
zero. Leans (a) structural — `forall`/`implies` wrapper not decomposed
— leaning is not a ruling. Diagnostic still required: wrap an
already-solved goal in `(forall n (implies (greater n 1) …))` on the
frozen tag, packs loaded, count partial matches. F does not repeat the
decoy/FLT pair on unchanged semantics. S/INT owns the ruling. F track
remains ACTIVE on tooling.
