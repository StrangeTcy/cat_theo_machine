PORT RECIPE — self-improvement S track onto experiment-5-frozen-r1

Rehearsed and verified locally. INT has ratified this recipe (fable 5) with
two corrections applied below.

--------------------------------------------------------------------------
base
--------------------------------------------------------------------------
experiment-5-frozen-r1 = ef571b688bcfb581bd3e65ec28a18f438ca32595

port branch (created by the executing hand):
arena/01a06cca-self-improve-r1-port

--------------------------------------------------------------------------
source commits, in order (referenced by hash; unaltered)
--------------------------------------------------------------------------
548c6f672de2de642b534b5c89a9a9ce6b65959e [S] trace mining
8738bcf30660279b2ae5304924b1dc5a58e68503 [S] rent gate for compressions
1e451888aa24b7814c11711f9c2a7241cbd31d2f [S] recursive turn
0e50bc179ca4aff8186a056db4f33e126461cca1 [S] invariant conjecture from fixed library
218a40f66d3aa1fb63c395f85db6202deb76f4c5 [S] learned-memory cycle for self-improvement

--------------------------------------------------------------------------
conflict map (measured, reproduced twice)
--------------------------------------------------------------------------
Every commit conflicts in testsuite.py only, at the registration hunk, at the
'# --- end [S] ---' boundary. labels.py and research.py are clean on every
commit. One mechanical hunk per commit, five resolutions total, never a
second hunk, never a new file.

  548c6f6 trace mining       testsuite.py: 1 hunk
  8738bcf rent gate          testsuite.py: 1 hunk
  1e45188 recursive turn     testsuite.py: 1 hunk
  0e50bc1 invariant library  testsuite.py: 1 hunk
  218a40f memory cycle       testsuite.py: 1 hunk

Resolution (deltas 1+2): keep the r1 track-partitioned registration block and
place each registration inside '# --- [S] --- ... # --- end [S] ---', in class
form (SelfImprovementXTest, not SelfImprovementXTest(graph)) to match the r1
_register_test signature. Script: rehearse_resolve.py <name>.

--------------------------------------------------------------------------
deltas, applied in the port commit (same commit, after the five cherry-picks)
--------------------------------------------------------------------------
delta 3 (SHARED, INT sign-off given):
  - labels.sync_from_namespace: add the five names after "InvarianceLabel",
  - persistence.SNAPSHOT_SYMBOL_NAMES: add the five names after
    "CorrespondenceLawLabel",
  - pins UNCHANGED at 40 / 198 / 18. The earlier 40->45 / 198->203 / 18->23
    figure described the unregistered failure mode (the completeness test
    fails on it by design, pins move only downward). Registering keeps them
    put. The five labels are genuine leaf ConstructorLabel singletons and
    transient markers (never persisted, never in the provenance map), so the
    snapshot-table entries are benign over-registration forced by the
    no-upward-pin rule. Script: rehearse_deltas.py.

delta 4 (same port commit):
  - TestShardCursorPinTest.EXPECTED_GUARD_COUNT: 305 -> 310.
  - hard pin holds unchanged: EXPECTED_CURSOR_INDEX 218, EXPECTED_SHARD 0,
    because the registrations land after every existing test.

--------------------------------------------------------------------------
verified green on r1 (PYTHONPATH=/tmp, venv gmpy2+pyyaml)
--------------------------------------------------------------------------
label_registration_completeness_test  PASS
test_shard_cursor_pin_test            PASS
self_improvement_trace_mining_test    PASS
self_improvement_rent_gate_test       PASS
self_improvement_recursive_turn_test  PASS
invariant_conjecture_test             PASS
self_improvement_memory_cycle_test    PASS

Harness: run_ported.py.

--------------------------------------------------------------------------
shard assignment on r1 (registrations at cursors 305..309)
--------------------------------------------------------------------------
trace(305), recursive(307), memory(309) -> shard 1
rent(306), invariant(308) -> shard 0

--------------------------------------------------------------------------
shard-1 halt discrepancy — RESOLVED, world (a), with stated grounds
--------------------------------------------------------------------------
The discrepancy resolves as: the halt is lineage-dependent, not
environment-dependent, and the port base r1 is the lineage WITHOUT the halt.

Causal chain, established by reading + one runtime probe on r1 this turn:

  - The fragile extraction is INSTALL-TIME. It lives in
    ConversePropositionTest.__init__ (testsuite.py:7449 on r1), which runs
    at registration inside install_default_tests: r1's _register_test
    constructs the edge (computation_edge(graph)) when the name filter
    admits it, and a full shard run sets no filter. Registration is
    guarded by TestShardAccept, so the test constructs only on its owning
    shard (shard 1).

  - The abort is caused by commit 41e8078's genus_body_bare template
    pattern '?a' shadowing every bare number word (d528573 "Defect: a
    bare-variable template law shadowed every number word"). With it, the
    canonical equality sentence 'is two plus two equal to four' returned
    NotUnderstood at depth 3, and the unconditional four-Tail walk in the
    test ran off the end -> AttributeError aborting the whole shard-1
    install.

  - d528573 removes the shadowing template and is IN the r1 lineage
    (ancestor of ef571b6; the commit's own message: 'the former unblocks
    shard 1, which had been aborting during installation'). It is NOT in
    experiment-4 (06c0e52), which is why this session's full-shard-1 runs
    on experiment-4 and on the 06c0e52-based branch crashed/hung at the
    converse install.

  - The r1 lineage does NOT contain the later sibling-lineage guard hoist
    3cac74c ("ConversePropositionTest guards Understood before navigating
    outcome tails", branched from 41e8078, not an ancestor of ef571b6).
    r1 still has the unconditional four-Tail walk. It does not matter:
    on r1 the outcome is Understood again, so the walk succeeds.

Runtime probe, this session, on r1 (ef571b6), venv gmpy2+pyyaml:
    Converse('is two plus two equal to four') -> outcome head Understood,
    5 navigable pair slots; ConversePropositionTest constructs and returns
    truth_value (PASS).

Prediction for the gate (world a): the port branch, based on r1, installs
shard 1 fully; the three shard-1 registrations (trace 305, recursive 307,
memory 309) ARE reached and must be green; the comparison is 145/5/2
unchanged. World (b) is expected only if the executing environment's
Converse regresses to NotUnderstood for the equality sentence.

One-command check (seconds, before the expensive shard run) to confirm the
world without running the full shard:
    python3 tools/run_named_tests.py converse_proposition_test
on the port branch. PASS (or an ordinary failing-test report) => world (a).
An install abort at testsuite.py:7449 => world (b), and the gate baseline
statement needs the corrected caveat before any pass/fail claim.

--------------------------------------------------------------------------
remaining gate items (executing hand)
--------------------------------------------------------------------------
1. create arena/01a06cca-self-improve-r1-port from ef571b6
2. apply this recipe (five cherry-picks + four deltas)
3. full shard 0: expect 147/3/2 plus rent(306) and invariant(308) green
4. full shard 1: prediction world (a) — 145/5/2 unchanged, trace/recursive/memory green; the one-command converse check above confirms before the run
5. per-entry OPEN mapping, explicit (no arithmetic-only claim)
6. memory-cycle test on the r1 persistence path (D3 interaction), or a
   recorded machine term that the path is absent
7. merge-tree against INT's landing tip at execution time
8. push; the five source commits remain the port source, referenced by hash

Note (D1 fix provenance): the sibling-lineage guard hoist 3cac74c is NOT
required for the port — the r1 lineage carries d528573, which removes the
cause rather than guarding the symptom. The executing hand does not need to
bring 3cac74c in.
